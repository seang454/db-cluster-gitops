global:
  clusterIdentity: testing
  backupBucket: a8s-clusterdb-backup
gateway:
  enabled: true
postgresql:
  enabled: true
  externalAccess:
    publicHostnames:
    - testing.seang.shop
  cluster:
    imageName: ghcr.io/cloudnative-pg/postgresql:16.14
    instances: 3
    bootstrap:
      database: testdb
      owner: app_testing
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 1Gi
  storage:
    size: 4Gi
    wal:
      enabled: false
  pooler:
    enabled: true
  externalSecretRef: testing-postgresql-app
  superuserExternalSecretRef: testing-postgresql-credentials
