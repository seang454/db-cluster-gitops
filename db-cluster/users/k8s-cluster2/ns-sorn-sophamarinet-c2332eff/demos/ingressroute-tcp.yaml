apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: demos-tcp
  namespace: ns-sorn-sophamarinet-c2332eff
spec:
  entryPoints:
  - postgresql
  routes:
  - match: HostSNI(`demos.seang.shop`)
    services:
    - name: demos-postgresql-pooler-rw
      port: 5432
  tls:
    passthrough: true
