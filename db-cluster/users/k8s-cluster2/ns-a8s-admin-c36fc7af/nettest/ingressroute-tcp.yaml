apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: nettest-tcp
  namespace: ns-a8s-admin-c36fc7af
spec:
  entryPoints:
  - postgresql
  routes:
  - match: HostSNI(`nettest.seang.shop`)
    services:
    - name: nettest-postgresql-pooler-rw
      port: 5432
  tls:
    passthrough: true
