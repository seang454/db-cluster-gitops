global:
  clusterIdentity: postgresbmevbg
gateway:
  enabled: true
postgresql:
  enabled: true
  externalAccess:
    enabled: true
    publicHostnames:
    - postgrest.seang.shop
  tls:
    enabled: true
    mode: operator
  cluster:
    bootstrap:
      database: posgrest
      owner: app_deploy_posgrest
    resources:
      requests:
        cpu: 250m
        memory: 768Mi
      limits:
        cpu: 750m
        memory: 2Gi
  storage:
    size: 4Gi
  externalSecretRef: postgresbmevbg-postgresql-app
  superuserExternalSecretRef: postgresbmevbg-postgresql-credentials
