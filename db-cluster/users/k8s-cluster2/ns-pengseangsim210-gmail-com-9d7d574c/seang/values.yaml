global:
  clusterIdentity: seang
gateway:
  enabled: true
cassandra:
  enabled: true
  externalAccess:
    publicHostnames:
    - seang.seang.shop
  storage:
    size: 4Gi
  cluster:
    image: k8ssandra/cass-management-api:5.0
    serverVersion: 5.0.3
    instances: 3
    config:
      clusterName: seang
      datacenter: dc1
      requireClientAuth: false
    resources:
      requests:
        cpu: 300m
        memory: 768Mi
      limits:
        cpu: 1000m
        memory: 2Gi
    probes:
      readinessInitialDelaySeconds: 300
      readinessTimeoutSeconds: 20
      livenessInitialDelaySeconds: 600
      livenessTimeoutSeconds: 20
      startupDelaySeconds: 900
  externalSecretRef: seang-cassandra-credentials
