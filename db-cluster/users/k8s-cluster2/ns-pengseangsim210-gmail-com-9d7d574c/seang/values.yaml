global:
  clusterIdentity: seang
  backupBucket: a8s-clusterdb-backup
gateway:
  enabled: true
cassandra:
  enabled: true
  externalAccess:
    publicHostnames:
    - seang.seang.shop
  storage:
    size: 4Gi
  connection:
    keyspace: database
    username: app_seang
  cluster:
    image: k8ssandra/cass-management-api:4.1.3
    serverVersion: 4.1.3
    instances: 3
    config:
      clusterName: seang
      datacenter: dc1
      requireClientAuth: false
    resources:
      requests:
        cpu: 300m
        memory: 768Mi
      limits:
        cpu: 1000m
        memory: 2Gi
    probes:
      readinessInitialDelaySeconds: 300
      readinessTimeoutSeconds: 20
      livenessInitialDelaySeconds: 600
      livenessTimeoutSeconds: 20
      startupDelaySeconds: 900
  externalSecretRef: seang-cassandra-credentials
